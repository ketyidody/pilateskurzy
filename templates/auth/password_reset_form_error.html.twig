{% extends 'base.html.twig' %}

{% block content %}
    <!-- Hero section -->
    <div class="section fp-auto-height" id="hero-section">
        <div class="hero-slider owl-carousel">
            <div class="hs-item set-bg" data-setbg="{{ asset('img/slider/yoga-7.jpg') }}">
                <div class="container text-white">
                    <h3>Chyba</h3>
                    {% if message is not empty %}
                        <div class="row">
                            <div class="col-md-12 user-notification user-notification-error">
                                {{ message }}
                            </div>
                        </div>
                    {% endif %}
                    <div class="col-md-6 margin-auto">
                        <a href="{{ path('auth_login') }}" class="site-btn"><span>Späť na prihlásenie</span></a>
                    </div>
                </div>
            </div>
        </div>
        <div class="slide-num-holder" id="snh-1"></div>
        {{ include('pages/partial/contact-link.html.twig') }}
    </div>
    <!-- Hero section end -->
{% endblock %}

